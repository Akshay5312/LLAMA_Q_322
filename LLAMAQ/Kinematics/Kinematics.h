template <int inDOF, int outDOF>
class Kinematics{
    private:

    public:
        Kinematics(){};
        ~Kinematics(){};

        //virtual VectorXd<outDOF> FK(VectorXd<inDOF> Q)
        //virtual MatrixXd<outDOF, inDOF> Jacobian(VectorXd<inDOF> Q) 
};